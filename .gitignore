.DS_Store
Thumbs.db
*.log
.env
.env.local
node_modules/
.tmp/
pb_data/
# PocketBase server executable (32 MB) — download from pocketbase.io releases, don't commit
**/apps/pocketbase/pocketbase

# Holds the real Google OAuth client secret in plaintext — never commit
Google Sign-In Json Payment Signup/
